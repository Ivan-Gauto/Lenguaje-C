#include <stdio.h>

int sumar(int, int);
void mostrarResultado();

int main() {
	mostrarResultado();
}

int sumar(parametro1, parametro2) {
	return parametro1 + parametro2;
}

void mostrarResultado() {
	printf("La suma de 5 + 10 %c %d", 61, sumar(5, 10));
}
